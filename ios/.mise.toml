# Pins the Tuist version for this repo so the `tuist` mise shim resolves.
# Everyone generating the project must use this version, so project.pbxproj
# does not churn between machines.
[tools]
tuist = "4.208.0"
